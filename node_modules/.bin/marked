#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_marked@0.6.2@marked/bin/marked" "$@"
  ret=$?
else 
  node  "$basedir/../_marked@0.6.2@marked/bin/marked" "$@"
  ret=$?
fi
exit $ret
